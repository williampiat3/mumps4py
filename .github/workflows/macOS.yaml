name: CI MUMPS4PY (macOS)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test-macos:
    runs-on: macos-latest

    env:
      MUMPS_SOLVERS: dmumps,zmumps,smumps,cmumps
      MUMPS_INC: ${{ github.workspace }}/MUMPS_5.7.3/include
      MUMPS_LIB: ${{ github.workspace }}/MUMPS_5.7.3/lib

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install system packages + OpenMPI + gfortran
      run: |
        brew update
        brew install open-mpi openblas lapack cmake metis gcc
        GFORTRAN_PATH=$(find $(brew --prefix gcc)/bin -name 'gfortran-*' | head -n 1)
        if [ -z "$GFORTRAN_PATH" ]; then
          echo "Error: gfortran not found after installing gcc"
          exit 1
        fi
        GFORTRAN_BIN=$(basename $GFORTRAN_PATH)
        echo "Found gfortran: $GFORTRAN_BIN"
        $GFORTRAN_BIN --version
        echo "$(brew --prefix gcc)/bin" >> $GITHUB_PATH
        echo "PATH after update: $PATH"
        which $GFORTRAN_BIN

    - name: Set METIS and OpenBLAS paths
      run: |
        echo "METIS_DIR=$(brew --prefix metis)" >> $GITHUB_ENV
        echo "OPENBLAS_DIR=$(brew --prefix openblas)" >> $GITHUB_ENV

    - name: Check METIS and OpenBLAS installation
      run: |
        # Verify METIS headers and libraries
        if [ ! -f "$METIS_DIR/include/metis.h" ]; then
          echo "Error: metis.h not found in $METIS_DIR/include"
          ls -l "$METIS_DIR/include"
          exit 1
        fi
        if [ ! -f "$METIS_DIR/lib/libmetis.dylib" ] && [ ! -f "$METIS_DIR/lib/libmetis.a" ]; then
          echo "Error: libmetis not found in $METIS_DIR/lib"
          ls -l "$METIS_DIR/lib"
          exit 1
        fi
        echo "METIS header found: $METIS_DIR/include/metis.h"
        echo "METIS library found in: $METIS_DIR/lib"
        # Verify OpenBLAS
        if [ ! -f "$OPENBLAS_DIR/lib/libopenblas.dylib" ] && [ ! -f "$OPENBLAS_DIR/lib/libopenblas.a" ]; then
          echo "Error: libopenblas not found in $OPENBLAS_DIR/lib"
          ls -l "$OPENBLAS_DIR/lib"
          exit 1
        fi
        echo "OpenBLAS library found in: $OPENBLAS_DIR/lib"

    - name: Install MUMPS 5.7.3 (parallel + shared with mpicc/mpif90, no OpenMP)
      run: |
        curl -L https://coin-or-tools.github.io/ThirdParty-Mumps/MUMPS_5.7.3.tar.gz -o MUMPS_5.7.3.tar.gz
        tar xzf MUMPS_5.7.3.tar.gz
        cd MUMPS_5.7.3
        cp Make.inc/Makefile.debian.PAR Makefile.inc

        # Remove SCOTCH-related settings
        sed -i '' '/^SCOTCH/d' Makefile.inc
        sed -i '' '/^ISCOTCH/d' Makefile.inc
        sed -i '' '/^LSCOTCH/d' Makefile.inc

        # Use mpicc/mpif90 with gfortran
        echo "CC = mpicc" >> Makefile.inc
        echo "FC = mpif90" >> Makefile.inc
        echo "FL = mpif90" >> Makefile.inc

        # Compiler and linker flags
        echo "CFLAGS = -O -fPIC -I${METIS_DIR}/include" >> Makefile.inc
        echo "LDFLAGS = -L${METIS_DIR}/lib -L${OPENBLAS_DIR}/lib" >> Makefile.inc
        echo "OPTC = -O -fPIC -I${METIS_DIR}/include" >> Makefile.inc
        echo "OPTF = -O2 -fallow-argument-mismatch -fPIC" >> Makefile.inc

        # External libraries
        echo "LIBBLAS = -L${OPENBLAS_DIR}/lib -lopenblas" >> Makefile.inc
        echo "LIBMETIS = -L${METIS_DIR}/lib -lmetis" >> Makefile.inc
        echo "IMETIS = -I${METIS_DIR}/include" >> Makefile.inc
        echo "SONAMEOPT = -install_name" >> Makefile.inc  # macOS-compatible option

        # Remove OpenMP, Scotch, and fix library extension
        find . -name "Makefile*" -exec sed -i '' 's/-fopenmp//g' {} \;
        find . -name "Makefile*" -exec sed -i '' 's/-Xpreprocessor -fopenmp//g' {} \;
        find . -name "Makefile*" -exec sed -i '' 's/-Dscotch//g' {} \;
        find . -name "Makefile*" -exec sed -i '' 's/-soname/-install_name/g' {} \;
        find . -name "Makefile*" -exec sed -i '' 's/\.so/.dylib/g' {} \;

        # Export linker flags to ensure METIS and OpenBLAS are found
        export LDFLAGS="-L${METIS_DIR}/lib -lmetis -L${OPENBLAS_DIR}/lib -lopenblas"

        # Build PORD
        cd PORD/lib
        make PLAT="" FPIC="-fPIC" CC="mpicc" CFLAGS="-O -fPIC -I${METIS_DIR}/include" LDFLAGS="${LDFLAGS}" AR="ar vr " RANLIB="ranlib" OUTC="-o " LIBEXT=".a" LIBEXT_SHARED=".dylib" libpord.dylib
        cd ../..

        # Compile additional sources
        mpicc -I./include -O -fPIC -I${METIS_DIR}/include -c ./src/graph.c -o ./src/graph.o || true
        mpicc -I./include -O -fPIC -I${METIS_DIR}/include -c ./src/gbipart.c -o ./src/gbipart.o || true
        mpicc -I./include -O -fPIC -I${METIS_DIR}/include -c ./src/gbisect.c -o ./src/gbisect.o || true

        # Build MUMPS
        make -j$(sysctl -n hw.ncpu) allshared

        # Organize headers and libraries
        mkdir -p include lib
        cp include/*.h include/
        cp *.mod include/ || true
        cp lib/*.a lib/ || true
        cp lib/*.dylib lib/ || true
        cp libseq/libmpiseq.a lib/ || true

    - name: Install Python packages
      run: |
        python -m pip install --upgrade pip
        pip install numpy mpi4py cython pytest

    - name: Build mumps4py (no OpenMP)
      run: |
        export CC=mpicc
        export CFLAGS="-O -fPIC -I${{ env.MUMPS_INC }} -I${{ env.METIS_DIR }}/include"
        export LDFLAGS="-L${{ env.MUMPS_LIB }} -L${{ env.METIS_DIR }}/lib -lmetis -L${{ env.OPENBLAS_DIR }}/lib -lopenblas"
        python setup.py build_ext --inplace --verbose
        pip install .

    - name: Run tests
      run: |
        python -c "import mumps4py; print(f'MUMPS4PY successfully imported from {mumps4py.__file__}')"
        mpiexec -n 2 pytest -v
